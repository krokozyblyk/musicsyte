# 🚨 СРОЧНО: Исправьте настройки в Railway!

## ❌ Проблема в ваших настройках:

1. **Root Directory:** `/api-gateway` (с слэшем) → должно быть `api-gateway` (без слэша)
2. **Builder:** `Railpack` (Default) → должно быть `DOCKERFILE`

---

## ✅ ИСПРАВЛЕНИЕ (2 минуты):

### 1. Откройте Railway:
https://railway.app → Ваш проект → Сервис `api-gateway` → **Settings**

### 2. Найдите раздел "Build" (Сборка)

### 3. Измените Builder:

**Сейчас:** `Railpack` (Default)

**Измените на:** `DOCKERFILE`

1. Нажмите на выпадающий список "Builder"
2. Выберите **"DOCKERFILE"** (не Railpack, не NIXPACKS)
3. Появится поле "Dockerfile Path"

### 4. Укажите Dockerfile Path:

**Введите:** `Dockerfile`

(или `api-gateway/Dockerfile` если Root Directory = `.`)

### 5. Найдите раздел "Source"

### 6. Измените Root Directory:

**Сейчас:** `/api-gateway` (с слэшем в начале)

**Измените на:** `api-gateway` (без слэша в начале)

1. Нажмите на поле "Root directory"
2. Удалите слэш `/` в начале
3. Должно быть: `api-gateway` (не `/api-gateway`)

### 7. Проверьте Start Command:

**Должно быть ПУСТЫМ** (используется из Dockerfile)

Если там что-то указано (например, `bash start.sh`) → **УДАЛИТЕ**

### 8. Сохраните и передеплойте:

1. Нажмите **"Save"** или **"Update"**
2. Нажмите **"Deploy"** или **"Redeploy"**

---

## 📋 Правильные настройки:

```
Source:
  Root Directory: api-gateway (БЕЗ слэша в начале!)

Build:
  Builder: DOCKERFILE (не Railpack!)
  Dockerfile Path: Dockerfile
  Build Command: (ПУСТО)

Deploy:
  Start Command: (ПУСТО)
```

---

## ⚠️ ВАЖНО:

- **Root Directory** = `api-gateway` (БЕЗ `/` в начале!)
- **Builder** = `DOCKERFILE` (не Railpack!)
- **Dockerfile Path** = `Dockerfile` (Railway найдет `api-gateway/Dockerfile`)
- **Start Command** = ПУСТО

---

## ✅ После исправления:

Railway будет:
1. Использовать Dockerfile из `api-gateway/Dockerfile`
2. Контекст сборки будет `api-gateway/`
3. Dockerfile скопирует `pom-standalone.xml` и `src` правильно
4. Сборка пройдет успешно! ✅

---

**Исправьте эти 2 настройки и ошибка исчезнет! 🎯**

