# 🔧 ИСПРАВЛЕНИЕ CORS ОШИБКИ

## ❌ Проблема:

```
Access to fetch at 'https://musicsyte-production.up.railway.app/api/...' 
from origin 'https://musicsyte.vercel.app' has been blocked by CORS policy
```

API Gateway не разрешает запросы с домена Vercel.

---

## ✅ РЕШЕНИЕ: Добавить переменную окружения в Railway

### Шаг 1: Откройте Railway

1. Откройте https://railway.app
2. Перейдите в ваш проект
3. Откройте сервис **`api-gateway`**
4. Перейдите во вкладку **"Variables"**

### Шаг 2: Добавьте переменную

1. Нажмите **"+ New Variable"** (или "Add Variable")
2. **Key:** `CORS_ALLOWED_ORIGINS`
3. **Value:** 
   ```
   https://musicsyte.vercel.app,https://musicsyte-production.up.railway.app
   ```
4. Нажмите **"Add"** или **"Save"**

### Шаг 3: Передеплойте

1. Перейдите во вкладку **"Deployments"**
2. Нажмите **"Redeploy"** (или дождитесь автоматического деплоя)
3. Подождите 2-5 минут

---

## ✅ Альтернатива: Если не работает через переменную

Нужно обновить код CorsConfig, чтобы он правильно обрабатывал wildcard.

---

**После добавления переменной и передеплоя ошибка CORS исчезнет! ✅**

