# 🚨 СРОЧНО: Исправьте настройки в Railway!

## ❌ Проблема: "The executable `bash` could not be found"

**Причина:** В настройках Railway указан Start Command с `bash` или `start.sh`

---

## ✅ БЫСТРОЕ ИСПРАВЛЕНИЕ (2 минуты):

### 1. Откройте Railway:
https://railway.app → Ваш проект → Сервис `api-gateway`

### 2. Перейдите в Settings:

### 3. Найдите раздел "Deploy" или "Start Command"

### 4. УДАЛИТЕ или ИЗМЕНИТЕ Start Command:

**❌ УДАЛИТЕ это:**
- `bash api-gateway/start.sh`
- `bash start.sh`
- Любую команду с `bash`

**✅ Оставьте ПУСТЫМ** (используется из Dockerfile)

**ИЛИ укажите:**
- `java -jar app.jar`

### 5. Сохраните и передеплойте:

1. Нажмите **"Save"**
2. Нажмите **"Deploy"** или **"Redeploy"**

---

## 📋 Правильные настройки:

```
Root Directory: api-gateway (или .)
Builder: DOCKERFILE
Dockerfile Path: api-gateway/Dockerfile
Build Command: (ПУСТО)
Start Command: (ПУСТО) ← ВОТ ЭТО ВАЖНО!
```

---

## ⚠️ ВАЖНО:

Dockerfile уже правильно настроен с `ENTRYPOINT ["java", "-jar", "app.jar"]`, поэтому Start Command должен быть **ПУСТЫМ**!

---

**После этого ошибка исчезнет! ✅**

