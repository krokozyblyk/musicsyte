# 🔄 Как работает деплой на Railway

## 📌 Главное правило:

**Локальные изменения НЕ попадают на сайт автоматически!**

Нужно отправить изменения в GitHub, и только тогда Railway обновит сайт.

---

## 🔄 Процесс обновления сайта:

### 1️⃣ Вы меняете файлы локально
```
Ваш компьютер
├── Dockerfile (изменили)
├── api-gateway/railway.json (изменили)
└── ... другие файлы
```

### 2️⃣ Отправляете в GitHub
```bash
# В терминале (в корне проекта)
git add .
git commit -m "Исправил Dockerfile для api-gateway"
git push
```

### 3️⃣ Railway автоматически видит изменения
- Railway подключен к вашему GitHub репозиторию
- Когда вы делаете `git push`, Railway получает уведомление
- Railway автоматически запускает сборку (build)

### 4️⃣ Railway деплоит новую версию
- Собирает проект заново
- Запускает новую версию
- **Сайт обновляется!** ✅

---

## ⚠️ ВАЖНО:

### ❌ НЕ работает:
- Просто сохранить файл локально → сайт НЕ изменится
- Изменить файл в редакторе → сайт НЕ изменится
- Закоммитить, но не сделать `git push` → сайт НЕ изменится

### ✅ РАБОТАЕТ:
- `git add .` → `git commit` → `git push` → сайт обновится!

---

## 🔍 Как проверить, что изменения попали на сайт:

### Вариант 1: В Railway Dashboard
1. Откройте ваш проект в Railway
2. Перейдите в сервис (например, `api-gateway`)
3. Во вкладке **"Deployments"** увидите новый деплой
4. Статус будет **"Building"** → **"Deploying"** → **"Active"**

### Вариант 2: В логах
1. В Railway откройте вкладку **"Logs"**
2. Увидите процесс сборки и деплоя
3. Если есть ошибки - они будут видны здесь

### Вариант 3: На сайте
1. Откройте ваш сайт
2. Проверьте, что изменения применились
3. Если не видно - подождите 2-3 минуты (деплой может занять время)

---

## 🚀 Быстрая команда для обновления:

```bash
# Одна команда для всего:
git add . && git commit -m "Обновление" && git push
```

---

## 📝 Пример работы:

### Сценарий: Вы исправили Dockerfile

1. **Локально:** Открыли `Dockerfile`, исправили ошибку, сохранили
2. **GitHub:** 
   ```bash
   git add Dockerfile
   git commit -m "Исправил Dockerfile для api-gateway"
   git push
   ```
3. **Railway:** 
   - Видит новый коммит в GitHub
   - Автоматически запускает сборку
   - Собирает проект с новым Dockerfile
   - Деплоит новую версию
4. **Сайт:** Обновляется через 2-5 минут ✅

---

## 🔧 Настройка автоматического деплоя:

Railway по умолчанию деплоит при каждом `git push` в ветку `main` (или `master`).

Если нужно изменить ветку:
1. Откройте настройки сервиса в Railway
2. **Settings** → **Source**
3. Выберите нужную ветку

---

## ❓ Частые вопросы:

### Q: Я изменил файл, но сайт не обновился. Почему?
**A:** Вы забыли сделать `git push`. Локальные изменения не попадают на сайт автоматически.

### Q: Сколько времени занимает деплой?
**A:** Обычно 2-5 минут (сборка + деплой).

### Q: Можно ли откатить изменения?
**A:** Да! В Railway есть вкладка **"Deployments"**, где можно откатиться к предыдущей версии.

### Q: Как проверить, что изменения в GitHub?
**A:** Откройте ваш репозиторий на GitHub.com и проверьте последний коммит.

---

## ✅ Чеклист перед деплоем:

- [ ] Изменил файлы локально
- [ ] Проверил, что изменения правильные
- [ ] Сделал `git add .`
- [ ] Сделал `git commit -m "Описание изменений"`
- [ ] Сделал `git push`
- [ ] Проверил в Railway, что деплой запустился
- [ ] Подождал 2-5 минут
- [ ] Проверил сайт - изменения применились

---

**Готово! Теперь вы знаете, как работает деплой! 🎉**

