# 🔧 Исправление ошибок 503: Service Unavailable

## ❌ Проблема:

Ошибки типа:
- `User Service is temporarily unavailable`
- `Artist Service is temporarily unavailable`
- `Album Service is temporarily unavailable`
- `Track Service is temporarily unavailable`

**Причина:** API Gateway работает, но микросервисы не задеплоены на Railway.

---

## ✅ РЕШЕНИЕ: Задеплоить микросервисы

### Минимальный набор для работы сайта:

1. **User Service** - для регистрации/входа
2. **Music Catalog Service** - для артистов, альбомов, треков

---

## 🚀 Быстрый деплой User Service (5 минут)

### 1. Создайте сервис в Railway:

1. В Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите ваш репозиторий: `krokozyblyk/musicsyte`

### 2. Настройки:

**Settings:**
- **Service Name:** `user-service`
- **Root Directory:** `user-service`
- **Builder:** `DOCKERFILE` (или NIXPACKS)
- **Build Command:** (пусто, если используете Dockerfile)
- **Start Command:** (пусто, если используете Dockerfile)

**ИЛИ если используете NIXPACKS:**
- **Build Command:** `cd user-service && mvn clean package -DskipTests`
- **Start Command:** `cd user-service && java -jar target/user-service-1.0.0.jar`

### 3. Переменные окружения (Variables):

Добавьте переменные из PostgreSQL (из вашей базы данных):
```
SERVER_PORT=8081
SPRING_PROFILES_ACTIVE=prod
SPRING_DATASOURCE_URL=jdbc:postgresql://[ваш-хост]:5432/[ваша-бд]
SPRING_DATASOURCE_USERNAME=[ваш-юзер]
SPRING_DATASOURCE_PASSWORD=[ваш-пароль]
EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
```

### 4. Деплой:

Нажмите **"Deploy"** и дождитесь завершения.

---

## 🚀 Деплой Music Catalog Service (5 минут)

### 1. Создайте сервис:

1. **"+ New"** → **"GitHub Repo"**
2. Выберите репозиторий

### 2. Настройки:

- **Service Name:** `music-catalog-service`
- **Root Directory:** `music-catalog-service`
- **Builder:** `DOCKERFILE` (или NIXPACKS)
- Те же переменные БД, но `SERVER_PORT=8082`

### 3. Деплой:

Нажмите **"Deploy"**

---

## ⚠️ ВАЖНО: Нужны ли все сервисы?

Если у вас нет базы данных или других сервисов, можно:

### Вариант 1: Задеплоить только основные сервисы
- User Service
- Music Catalog Service

### Вариант 2: Использовать моки/заглушки
- Изменить API Gateway, чтобы возвращать тестовые данные
- Или использовать локальные данные в frontend

### Вариант 3: Задеплоить все сервисы
- Config Server
- Eureka Server
- User Service
- Music Catalog Service
- И остальные...

---

## 📋 Чеклист:

- [ ] PostgreSQL задеплоен на Railway
- [ ] User Service задеплоен
- [ ] Music Catalog Service задеплоен
- [ ] Переменные окружения добавлены
- [ ] Сервисы активны (статус "Active")

---

## 🎯 Минимальный набор для работы:

1. **PostgreSQL** (база данных)
2. **User Service** (регистрация/вход)
3. **Music Catalog Service** (артисты, альбомы, треки)

После деплоя этих сервисов сайт должен заработать! ✅

---

**Начните с деплоя User Service и Music Catalog Service! 🚀**

