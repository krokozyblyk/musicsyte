# ⚡ Быстрый деплой на Railway.app (30 минут)

## 🎯 Цель: Задеплоить проект с SSL/HTTPS для отчета

---

## 📋 Шаг 1: Регистрация (5 минут)

1. Перейдите на https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub (создайте аккаунт, если нет)

---

## 📋 Шаг 2: Подготовка GitHub репозитория (5 минут)

1. Создайте репозиторий на GitHub (если еще нет)
2. Загрузите ваш проект:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/music-project.git
   git push -u origin main
   ```

---

## 📋 Шаг 3: Деплой PostgreSQL (5 минут)

1. В Railway нажмите "New Project"
2. Выберите "Add Database" → "PostgreSQL"
3. Railway создаст БД автоматически
4. **ВАЖНО:** Скопируйте переменные окружения:
   - Откройте базу данных
   - Вкладка "Variables"
   - Скопируйте все переменные (они понадобятся для сервисов)

---

## 📋 Шаг 4: Деплой Config Server (3 минуты)

1. В проекте нажмите "New Service" → "GitHub Repo"
2. Выберите ваш репозиторий
3. В настройках:
   - **Root Directory:** `config-server`
   - **Build Command:** `cd config-server && mvn clean package -DskipTests`
   - **Start Command:** `cd config-server && java -jar target/config-server-0.0.1-SNAPSHOT.jar`
4. Добавьте переменные окружения:
   ```
   SPRING_PROFILES_ACTIVE=prod
   SERVER_PORT=8888
   ```
5. Нажмите "Deploy"

---

## 📋 Шаг 5: Деплой Eureka Server (3 минуты)

1. "New Service" → "GitHub Repo"
2. **Root Directory:** `eureka-server`
3. **Build Command:** `cd eureka-server && mvn clean package -DskipTests`
4. **Start Command:** `cd eureka-server && java -jar target/eureka-server-0.0.1-SNAPSHOT.jar`
5. Переменные окружения:
   ```
   SPRING_PROFILES_ACTIVE=prod
   SERVER_PORT=8761
   EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
   ```

---

## 📋 Шаг 6: Деплой API Gateway (5 минут) ⭐ ГЛАВНЫЙ ШАГ

1. "New Service" → "GitHub Repo"
2. **Root Directory:** `api-gateway`
3. **Build Command:** `cd api-gateway && mvn clean package -DskipTests`
4. **Start Command:** `cd api-gateway && java -jar target/api-gateway-0.0.1-SNAPSHOT.jar`
5. Добавьте переменные окружения из PostgreSQL (из шага 3)
6. **ВАЖНО:** В настройках сервиса:
   - Откройте вкладку "Settings"
   - Раздел "Networking"
   - Нажмите "Generate Domain"
   - Railway создаст домен типа: `api-gateway-production.up.railway.app`
   - **SSL настроится автоматически!** ✅

---

## 📋 Шаг 7: Деплой Frontend на Vercel (5 минут)

### 7.1. Подготовка

1. В `frontend/js/api.js` измените базовый URL:
   ```javascript
   // Найдите строку с API_BASE_URL
   const API_BASE_URL = 'https://your-api-gateway-domain.railway.app';
   // Замените на ваш домен из Railway
   ```

2. Закоммитьте изменения:
   ```bash
   git add frontend/js/api.js
   git commit -m "Update API URL for production"
   git push
   ```

### 7.2. Деплой на Vercel

1. Перейдите на https://vercel.com
2. Войдите через GitHub
3. "Add New Project"
4. Выберите ваш репозиторий
5. Настройки:
   - **Framework Preset:** Other
   - **Root Directory:** `frontend`
   - **Build Command:** (оставьте пустым)
   - **Output Directory:** `frontend`
6. Нажмите "Deploy"
7. Vercel автоматически создаст домен с HTTPS! ✅

---

## 📋 Шаг 8: Проверка (2 минуты)

1. Откройте ваш сайт на Vercel
2. Проверьте, что в адресной строке есть 🔒 (HTTPS)
3. Проверьте работу API (откройте консоль браузера F12)

---

## ✅ Готово!

Теперь у вас:
- ✅ Сайт доступен в интернете
- ✅ SSL сертификат установлен
- ✅ HTTPS работает
- ✅ Можно делать скриншоты для отчета

---

## 📸 Скриншоты для отчета

Сделайте скриншоты:

1. **Railway Dashboard** - все сервисы запущены
2. **API Gateway Settings** - домен с HTTPS
3. **Vercel Dashboard** - фронтенд задеплоен
4. **Открытый сайт в браузере** - с 🔒 в адресной строке
5. **Проверка SSL** - https://www.ssllabs.com/ssltest/

---

## 🆘 Если что-то не работает

### Проблема: Сервис не запускается
- Проверьте логи в Railway (вкладка "Deployments" → "View Logs")
- Убедитесь, что все переменные окружения добавлены

### Проблема: Ошибка подключения к БД
- Проверьте, что переменные БД скопированы правильно
- Убедитесь, что БД запущена

### Проблема: Frontend не подключается к API
- Проверьте CORS настройки в API Gateway
- Убедитесь, что URL в `api.js` правильный

---

**Время выполнения: ~30 минут** ⏱️

