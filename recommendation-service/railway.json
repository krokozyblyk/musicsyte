{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd recommendation-service && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "cd recommendation-service && java -jar target/recommendation-service-1.0.0.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

