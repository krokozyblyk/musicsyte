# 🚀 Руководство по хостингу и настройке SSL/HTTPS

## 📋 Содержание
1. [Выбор хостинга](#выбор-хостинга)
2. [Подготовка проекта к деплою](#подготовка-проекта-к-деплою)
3. [Вариант 1: Railway.app (РЕКОМЕНДУЕТСЯ)](#вариант-1-railwayapp-рекомендуется)
4. [Вариант 2: Render.com](#вариант-2-rendercom)
5. [Вариант 3: VPS хостинг (Beget, Hostland)](#вариант-3-vps-хостинг-beget-hostland)
6. [Настройка SSL/HTTPS](#настройка-sslhttps)
7. [Отчет о проделанной работе](#отчет-о-проделанной-работе)

---

## 🎯 Выбор хостинга

### ⚠️ Важная информация

Ваш проект - это **микросервисная архитектура** с 10 Spring Boot сервисами. Обычные веб-хосты (InfinityFree, Beget для PHP) **НЕ ПОДХОДЯТ** для такого проекта.

### ✅ Рекомендации по выбору:

#### 🥇 **ЛУЧШИЙ ВАРИАНТ: Railway.app**
- ✅ **Бесплатный тариф** (500 часов/месяц)
- ✅ Поддержка Docker и Spring Boot
- ✅ Автоматический SSL сертификат (Let's Encrypt)
- ✅ Простое развертывание
- ✅ Поддержка PostgreSQL
- ✅ HTTPS из коробки
- 🔗 **Сайт:** https://railway.app

#### 🥈 **Альтернатива: Render.com**
- ✅ Бесплатный тариф
- ✅ Автоматический SSL
- ✅ Поддержка Docker
- ⚠️ Медленнее бесплатного тарифа
- 🔗 **Сайт:** https://render.com

#### 🥉 **Если нужен из вашего списка: VPS от Beget/Hostland**
- ✅ Полный контроль
- ✅ Можно установить Docker
- ⚠️ Требует настройки вручную
- ⚠️ Платный (от 200-300₽/месяц)
- ⚠️ SSL нужно настраивать вручную

---

## 📦 Подготовка проекта к деплою

### 1. Создание Dockerfile для каждого сервиса

Для каждого микросервиса нужно создать Dockerfile. Начнем с API Gateway:

**`api-gateway/Dockerfile`:**
```dockerfile
FROM openjdk:17-jdk-slim

WORKDIR /app

# Копируем pom.xml и загружаем зависимости
COPY pom.xml .
COPY .mvn .mvn
COPY mvnw .
RUN chmod +x mvnw
RUN ./mvnw dependency:go-offline -B

# Копируем исходный код
COPY src ./src

# Собираем приложение
RUN ./mvnw clean package -DskipTests

# Запускаем приложение
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "target/api-gateway-0.0.1-SNAPSHOT.jar"]
```

### 2. Создание docker-compose для продакшена

**`docker-compose.prod.yml`:**
```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ${DB_USER:-username}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-durak123}
      POSTGRES_DB: ${DB_NAME:-music_prilo_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U username"]
      interval: 10s
      timeout: 5s
      retries: 5

  config-server:
    build: ./config-server
    ports:
      - "8888:8888"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    depends_on:
      - postgres

  eureka-server:
    build: ./eureka-server
    ports:
      - "8761:8761"
    depends_on:
      - config-server

  # Остальные сервисы аналогично...

volumes:
  postgres_data:
```

### 3. Настройка переменных окружения

Создайте файл `.env` для продакшена:
```env
DB_USER=username
DB_PASSWORD=your_secure_password
DB_NAME=music_prilo_db
DB_HOST=postgres
DB_PORT=5432
```

---

## 🚂 Вариант 1: Railway.app (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Регистрация

1. Перейдите на https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Подготовка проекта

1. Создайте репозиторий на GitHub (если еще нет)
2. Загрузите код в репозиторий

### Шаг 3: Деплой базы данных

1. В Railway нажмите "New Project"
2. Выберите "Add Database" → "PostgreSQL"
3. Railway автоматически создаст БД
4. Скопируйте переменные окружения:
   - `DATABASE_URL`
   - `PGHOST`
   - `PGPORT`
   - `PGUSER`
   - `PGPASSWORD`
   - `PGDATABASE`

### Шаг 4: Деплой сервисов

#### 4.1. Деплой Config Server

1. В проекте Railway нажмите "New Service" → "GitHub Repo"
2. Выберите ваш репозиторий
3. В настройках:
   - **Root Directory:** `config-server`
   - **Build Command:** `mvn clean package -DskipTests`
   - **Start Command:** `java -jar target/config-server-0.0.1-SNAPSHOT.jar`
4. Добавьте переменные окружения:
   ```
   SPRING_PROFILES_ACTIVE=prod
   SERVER_PORT=8888
   ```

#### 4.2. Деплой Eureka Server

1. "New Service" → "GitHub Repo"
2. **Root Directory:** `eureka-server`
3. **Build Command:** `mvn clean package -DskipTests`
4. **Start Command:** `java -jar target/eureka-server-0.0.1-SNAPSHOT.jar`
5. Переменные окружения:
   ```
   SPRING_PROFILES_ACTIVE=prod
   SERVER_PORT=8761
   EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
   ```

#### 4.3. Деплой остальных сервисов

Аналогично для каждого сервиса:
- `user-service`
- `music-catalog-service`
- `notification-service`
- `analytics-service`
- `recommendation-service`
- `search-service`
- `social-service`

**Важно:** Для каждого сервиса добавьте переменные БД из шага 3.

#### 4.4. Деплой API Gateway

1. "New Service" → "GitHub Repo"
2. **Root Directory:** `api-gateway`
3. **Build Command:** `mvn clean package -DskipTests`
4. **Start Command:** `java -jar target/api-gateway-0.0.1-SNAPSHOT.jar`
5. **Generate Domain** → Railway автоматически создаст домен с HTTPS!

### Шаг 5: Настройка домена и SSL

1. В настройках API Gateway нажмите "Settings"
2. В разделе "Networking" нажмите "Generate Domain"
3. Railway автоматически:
   - Создаст домен (например: `api-gateway-production.up.railway.app`)
   - Установит SSL сертификат (Let's Encrypt)
   - Настроит HTTPS

### Шаг 6: Настройка Frontend

#### Вариант A: Статический хостинг (проще)

1. Используйте **Vercel** или **Netlify** (бесплатно)
2. Подключите репозиторий GitHub
3. **Root Directory:** `frontend`
4. **Build Command:** (не нужен, статика)
5. **Publish Directory:** `frontend`
6. В `frontend/js/api.js` измените базовый URL:
   ```javascript
   const API_BASE_URL = 'https://your-api-gateway-domain.railway.app';
   ```

#### Вариант B: Деплой на Railway

1. Создайте простой Node.js сервер для статики
2. Или используйте Nginx в Docker

---

## 🌐 Вариант 2: Render.com

### Шаг 1: Регистрация

1. Перейдите на https://render.com
2. Войдите через GitHub

### Шаг 2: Деплой PostgreSQL

1. "New" → "PostgreSQL"
2. Настройте:
   - **Name:** `music-db`
   - **Database:** `music_prilo_db`
   - **User:** `username`
   - **Region:** (выберите ближайший)
3. Сохраните переменные подключения

### Шаг 3: Деплой сервисов

1. "New" → "Web Service"
2. Подключите GitHub репозиторий
3. Настройки:
   - **Name:** `api-gateway`
   - **Root Directory:** `api-gateway`
   - **Environment:** `Java`
   - **Build Command:** `cd api-gateway && mvn clean package -DskipTests`
   - **Start Command:** `cd api-gateway && java -jar target/api-gateway-0.0.1-SNAPSHOT.jar`
4. Добавьте переменные окружения (из PostgreSQL)
5. Render автоматически создаст домен с HTTPS!

### Шаг 4: Настройка SSL

Render автоматически предоставляет SSL для всех сервисов. Просто:
1. В настройках сервиса → "Settings"
2. В разделе "Custom Domain" можно добавить свой домен
3. SSL настроится автоматически

---

## 🖥️ Вариант 3: VPS хостинг (Beget, Hostland)

### Шаг 1: Покупка VPS

1. Зарегистрируйтесь на Beget.com или Hostland.ru
2. Купите VPS тариф (минимум 1GB RAM, 1 CPU)
3. Получите доступ по SSH

### Шаг 2: Установка Docker

```bash
# Подключитесь по SSH
ssh root@your-server-ip

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установите Docker Compose
apt-get update
apt-get install docker-compose-plugin -y
```

### Шаг 3: Загрузка проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/music-project.git
cd music-project

# Или загрузите через SCP
```

### Шаг 4: Настройка docker-compose

Создайте `docker-compose.prod.yml` (см. выше)

### Шаг 5: Запуск

```bash
docker compose -f docker-compose.prod.yml up -d
```

### Шаг 6: Настройка Nginx как reverse proxy

```bash
apt-get install nginx certbot python3-certbot-nginx -y
```

Создайте `/etc/nginx/sites-available/music-platform`:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте:
```bash
ln -s /etc/nginx/sites-available/music-platform /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### Шаг 7: Получение SSL сертификата

```bash
# Получите SSL сертификат от Let's Encrypt
certbot --nginx -d your-domain.com

# Certbot автоматически:
# - Получит сертификат
# - Настроит Nginx для HTTPS
# - Настроит автообновление
```

---

## 🔒 Настройка SSL/HTTPS

### Автоматическая настройка (Railway/Render)

✅ **Автоматически!** Эти платформы предоставляют SSL из коробки.

### Ручная настройка (VPS)

#### Вариант 1: Let's Encrypt (бесплатно)

```bash
# Установите Certbot
apt-get install certbot python3-certbot-nginx -y

# Получите сертификат
certbot --nginx -d your-domain.com -d www.your-domain.com

# Проверьте автообновление
certbot renew --dry-run
```

#### Вариант 2: Cloudflare (бесплатно)

1. Зарегистрируйтесь на https://cloudflare.com
2. Добавьте ваш домен
3. Измените DNS записи на Cloudflare
4. Включите "SSL/TLS" → "Full" или "Full (strict)"
5. Cloudflare автоматически предоставит SSL

### Проверка SSL

После настройки проверьте:
- https://www.ssllabs.com/ssltest/ (проверка качества SSL)
- Откройте сайт в браузере → должен быть 🔒 замок

---

## 📝 Отчет о проделанной работе

### Структура отчета:

#### 1. Титульный лист
- Название работы
- ФИО студента
- Группа
- Дата

#### 2. Введение
- Цель работы
- Задачи

#### 3. Выбор хостинга
- Обоснование выбора
- Сравнение вариантов
- Выбранный вариант

#### 4. Подготовка проекта
- Изменения в коде
- Настройка переменных окружения
- Создание Dockerfile (если использовали)

#### 5. Процесс деплоя
- Пошаговое описание
- Скриншоты каждого шага
- Команды, которые выполняли

#### 6. Настройка SSL/HTTPS
- Как получили сертификат
- Настройка HTTPS
- Скриншоты настроек

#### 7. Результаты
- Скриншот открытого сайта в браузере
- Скриншот с 🔒 (HTTPS активен)
- Проверка SSL (ssllabs.com)

#### 8. Заключение
- Что получилось
- Какие были трудности
- Выводы

### Пример скриншотов для отчета:

1. **Регистрация на хостинге**
2. **Настройка проекта**
3. **Деплой сервисов**
4. **Настройка SSL**
5. **Открытие сайта в браузере (с HTTPS)**
6. **Проверка SSL на ssllabs.com**

---

## 🎯 Рекомендации для быстрого старта

### Для отчета (быстро и просто):

1. **Используйте Railway.app** - самый простой вариант
2. **Деплойте только API Gateway + Frontend** (для упрощения)
3. **Используйте встроенную БД Railway** (PostgreSQL)
4. **SSL настроится автоматически**

### Минимальный деплой для отчета:

1. API Gateway на Railway → получаете HTTPS домен
2. Frontend на Vercel/Netlify → получаете HTTPS домен
3. БД на Railway (PostgreSQL)

**Время:** ~30-60 минут

---

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Render Documentation](https://render.com/docs)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)
- [Spring Boot Production Deployment](https://spring.io/guides/gs/spring-boot-for-azure/)

---

## ⚠️ Важные замечания

1. **Бесплатные тарифы имеют ограничения:**
   - Railway: 500 часов/месяц
   - Render: сервисы "засыпают" после 15 минут бездействия

2. **Для продакшена рекомендуется:**
   - Платный тариф
   - Свой домен
   - Мониторинг
   - Резервное копирование БД

3. **Безопасность:**
   - Не коммитьте пароли в Git
   - Используйте переменные окружения
   - Регулярно обновляйте зависимости

---

## ✅ Чеклист перед сдачей отчета

- [ ] Проект задеплоен и доступен в интернете
- [ ] SSL сертификат установлен и работает
- [ ] Сайт открывается по HTTPS
- [ ] В браузере показывается 🔒 (безопасное соединение)
- [ ] Сделаны скриншоты всех этапов
- [ ] Отчет оформлен по структуре выше
- [ ] Проверка SSL на ssllabs.com пройдена

---

**Удачи с деплоем! 🚀**

