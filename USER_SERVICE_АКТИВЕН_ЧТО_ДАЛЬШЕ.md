# 🎉 User Service активен! Что делать дальше?

## ✅ Шаг 1: Проверьте работу User Service (2 минуты)

1. Откройте ваш сайт на Vercel: `https://musicsyte.vercel.app`
2. Попробуйте зарегистрироваться:
   - Нажмите "Войти" или "Регистрация"
   - Создайте аккаунт
   - Проверьте, что регистрация работает
3. Попробуйте войти:
   - Войдите с созданными данными
   - Проверьте, что вход работает

**Если регистрация/вход работают** → User Service работает правильно! ✅

---

## ✅ Шаг 2: Задеплойте Music Catalog Service (5 минут)

Для работы артистов, альбомов и треков нужен Music Catalog Service.

### 2.1. Создайте сервис в Railway:

1. В Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите репозиторий: `krokozyblyk/musicsyte`

### 2.2. Настройки:

**Settings:**
- **Service Name:** `music-catalog-service`
- **Root Directory:** `music-catalog-service`
- **Builder:** `NIXPACKS` (или `Railpack`)
- **Build Command:** `cp pom-standalone.xml pom.xml && mvn clean package -DskipTests`
- **Start Command:** `java -jar target/music-catalog-service-1.0.0.jar`

### 2.3. Переменные окружения (Variables):

Добавьте те же переменные, что и для User Service, но измените порт:

```
SERVER_PORT=8082
SPRING_PROFILES_ACTIVE=prod
SPRING_DATASOURCE_URL=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
SPRING_DATASOURCE_USERNAME=${PGUSER}
SPRING_DATASOURCE_PASSWORD=${PGPASSWORD}
EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
```

И все переменные из PostgreSQL (PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE, DATABASE_URL).

### 2.4. Деплой:

Нажмите **"Deploy"** и дождитесь завершения.

---

## ✅ Шаг 3: Создайте pom-standalone.xml для Music Catalog Service

Нужно создать standalone pom.xml для music-catalog-service (аналогично user-service).

**Или** можно изменить Root Directory на `.` (корень проекта) и использовать обычный pom.xml.

---

## ✅ Шаг 4: Проверьте работу сайта (2 минуты)

После деплоя Music Catalog Service:

1. Обновите страницу на Vercel (Ctrl+F5)
2. Проверьте, что:
   - Артисты загружаются
   - Альбомы загружаются
   - Треки загружаются
   - Поиск работает

---

## 📋 Чеклист:

- [x] API Gateway активен ✅
- [x] User Service активен ✅
- [ ] Music Catalog Service задеплоен
- [ ] Регистрация/вход работают
- [ ] Артисты/альбомы/треки загружаются
- [ ] Поиск работает

---

## 🎯 Минимальный набор для работы сайта:

1. ✅ **API Gateway** - работает
2. ✅ **User Service** - работает
3. ⏳ **Music Catalog Service** - нужно задеплоить

После деплоя Music Catalog Service сайт должен полностью заработать! 🚀

---

**Следующий шаг:** Задеплойте Music Catalog Service! 📝

