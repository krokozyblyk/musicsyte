# 🔧 ИСПРАВЛЕНИЕ: "Unable to access jarfile target/${RAILWAY_SERVICE_NAME}-1.0.0.jar"

## ❌ Проблема:

Railway пытается запустить JAR с переменной `${RAILWAY_SERVICE_NAME}`, которая не подставляется, или JAR файл находится в другом месте.

---

## ✅ РЕШЕНИЕ 1: Удалить Start Command в Railway UI

### В Railway UI:

1. Откройте Settings → раздел **"Deploy"**
2. Найдите **"Start Command"** (или "Custom Start Command")
3. **УДАЛИТЕ** все, что там указано (должно быть ПУСТО)
4. Сохраните и передеплойте

**Почему:** Dockerfile уже содержит `ENTRYPOINT ["java", "-jar", "app.jar"]`, поэтому Start Command должен быть ПУСТЫМ!

---

## ✅ РЕШЕНИЕ 2: Если Start Command пустой, но все равно ошибка

Проверьте, что Dockerfile правильно копирует JAR:

В `api-gateway/Dockerfile` должно быть:
```dockerfile
COPY --from=build /app/target/api-gateway-1.0.0.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

---

## ✅ РЕШЕНИЕ 3: Указать Start Command явно

Если ничего не помогает, в Railway UI укажите:

**Start Command:** `java -jar app.jar`

(НЕ `target/...`, а просто `app.jar` - потому что Dockerfile копирует JAR в `/app/app.jar`)

---

## 📋 Правильные настройки:

```
Root Directory: api-gateway (или .)
Builder: Dockerfile
Dockerfile Path: Dockerfile (или api-gateway/Dockerfile)
Build Command: (ПУСТО)
Start Command: (ПУСТО) ← ВОТ ЭТО ГЛАВНОЕ!
```

---

## ⚠️ ВАЖНО:

- **НЕ используйте** `target/${RAILWAY_SERVICE_NAME}-1.0.0.jar` - переменная не подставляется!
- **НЕ используйте** `target/api-gateway-1.0.0.jar` - JAR находится в `/app/app.jar` внутри контейнера
- **Используйте** пустой Start Command (Dockerfile сам запустит приложение)

---

**Удалите Start Command в Railway UI и ошибка исчезнет! ✅**

