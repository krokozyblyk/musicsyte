# 🚂 Пошаговая инструкция: Деплой на Railway.app

## ✅ Шаг 1: Подготовка проекта (5 минут)

### 1.1. Убедитесь, что проект на GitHub

Если проект еще не на GitHub:

```bash
# В корне проекта
git init
git add .
git commit -m "Prepare for Railway deployment"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
git push -u origin main
```

**Важно:** Замените `ВАШ_USERNAME` и `ВАШ_РЕПОЗИТОРИЙ` на ваши данные.

---

## ✅ Шаг 2: Создание проекта в Railway (2 минуты)

1. Откройте https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий
5. Railway создаст проект

**Скриншот:** [Сделайте скриншот созданного проекта]

---

## ✅ Шаг 3: Деплой PostgreSQL базы данных (3 минуты)

1. В вашем проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. **ВАЖНО:** Откройте базу данных и перейдите во вкладку **"Variables"**
5. **Скопируйте все переменные** - они понадобятся позже:
   - `PGHOST`
   - `PGPORT`
   - `PGUSER`
   - `PGPASSWORD`
   - `PGDATABASE`
   - `DATABASE_URL`

**Скриншот:** [Сделайте скриншот переменных окружения БД]

---

## ✅ Шаг 4: Деплой Config Server (5 минут)

### 4.1. Создание сервиса

1. В проекте Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите ваш репозиторий
3. Railway автоматически определит проект

### 4.2. Настройка сервиса

1. Откройте созданный сервис
2. Перейдите в **"Settings"**
3. Найдите раздел **"Service Name"** и переименуйте в: `config-server`
4. Найдите раздел **"Root Directory"** и укажите: `config-server`
5. Найдите раздел **"Build Command"** и укажите:
   ```
   cd config-server && mvn clean package -DskipTests
   ```
6. Найдите раздел **"Start Command"** и укажите:
   ```
   cd config-server && java -jar target/config-server-0.0.1-SNAPSHOT.jar
   ```

### 4.3. Переменные окружения

1. Перейдите во вкладку **"Variables"**
2. Добавьте переменные:
   ```
   SERVER_PORT=8888
   SPRING_PROFILES_ACTIVE=prod
   ```

### 4.4. Деплой

1. Нажмите **"Deploy"** или Railway автоматически начнет деплой
2. Дождитесь завершения (2-3 минуты)
3. Проверьте логи - должны быть сообщения об успешном запуске

**Скриншот:** [Сделайте скриншот успешного деплоя Config Server]

---

## ✅ Шаг 5: Деплой Eureka Server (5 минут)

### 5.1. Создание сервиса

1. **"+ New"** → **"GitHub Repo"**
2. Выберите ваш репозиторий

### 5.2. Настройка

1. **Settings** → **Service Name:** `eureka-server`
2. **Root Directory:** `eureka-server`
3. **Build Command:**
   ```
   cd eureka-server && mvn clean package -DskipTests
   ```
4. **Start Command:**
   ```
   cd eureka-server && java -jar target/eureka-server-0.0.1-SNAPSHOT.jar
   ```

### 5.3. Переменные окружения

В **"Variables"** добавьте:
```
SERVER_PORT=8761
SPRING_PROFILES_ACTIVE=prod
EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
```

### 5.4. Деплой

Нажмите **"Deploy"** и дождитесь завершения.

**Скриншот:** [Сделайте скриншот деплоя Eureka]

---

## ✅ Шаг 6: Деплой User Service (5 минут)

### 6.1. Создание и настройка

1. **"+ New"** → **"GitHub Repo"**
2. **Settings:**
   - **Service Name:** `user-service`
   - **Root Directory:** `user-service`
   - **Build Command:** `cd user-service && mvn clean package -DskipTests`
   - **Start Command:** `cd user-service && java -jar target/user-service-0.0.1-SNAPSHOT.jar`

### 6.2. Переменные окружения

В **"Variables"** добавьте **ВСЕ переменные из PostgreSQL** (из шага 3):
```
PGHOST=ваш_pghost
PGPORT=ваш_pgport
PGUSER=ваш_pguser
PGPASSWORD=ваш_pgpassword
PGDATABASE=ваш_pgdatabase
DATABASE_URL=ваш_database_url
```

Также добавьте:
```
SERVER_PORT=8081
SPRING_PROFILES_ACTIVE=prod
SPRING_DATASOURCE_URL=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
SPRING_DATASOURCE_USERNAME=${PGUSER}
SPRING_DATASOURCE_PASSWORD=${PGPASSWORD}
SPRING_CLOUD_CONFIG_URI=http://config-server:8888
EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
```

### 6.3. Деплой

Нажмите **"Deploy"**

---

## ✅ Шаг 7: Деплой Music Catalog Service (5 минут)

Аналогично User Service:

1. **"+ New"** → **"GitHub Repo"**
2. **Settings:**
   - **Service Name:** `music-catalog-service`
   - **Root Directory:** `music-catalog-service`
   - **Build Command:** `cd music-catalog-service && mvn clean package -DskipTests`
   - **Start Command:** `cd music-catalog-service && java -jar target/music-catalog-service-0.0.1-SNAPSHOT.jar`

3. **Variables:** (те же переменные, что и для User Service, но `SERVER_PORT=8082`)

---

## ✅ Шаг 8: Деплой остальных сервисов (по 3 минуты каждый)

Повторите для каждого сервиса:

- **notification-service** → порт 8083
- **analytics-service** → порт 8084
- **recommendation-service** → порт 8085
- **search-service** → порт 8086
- **social-service** → порт 8087

**Формула для каждого:**
1. "+ New" → "GitHub Repo"
2. Settings → Root Directory: `название-сервиса`
3. Build Command: `cd название-сервиса && mvn clean package -DskipTests`
4. Start Command: `cd название-сервиса && java -jar target/название-сервиса-0.0.1-SNAPSHOT.jar`
5. Variables: те же переменные БД + правильный порт

---

## ✅ Шаг 9: Деплой API Gateway (ГЛАВНЫЙ!) (5 минут)

### 9.1. Создание сервиса

1. **"+ New"** → **"GitHub Repo"**
2. Выберите ваш репозиторий

### 9.2. Настройка

1. **Settings:**
   - **Service Name:** `api-gateway`
   - **Root Directory:** `api-gateway`
   - **Build Command:** `cd api-gateway && mvn clean package -DskipTests`
   - **Start Command:** `cd api-gateway && java -jar target/api-gateway-0.0.1-SNAPSHOT.jar`

### 9.3. Переменные окружения

В **"Variables"** добавьте:
```
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=prod
SPRING_CLOUD_CONFIG_URI=http://config-server:8888
EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
```

### 9.4. ⭐ ВАЖНО: Создание домена с HTTPS

1. В настройках API Gateway найдите раздел **"Networking"**
2. Нажмите **"Generate Domain"**
3. Railway создаст домен типа: `api-gateway-production.up.railway.app`
4. **SSL настроится автоматически!** ✅

**Скриншот:** [Сделайте скриншот созданного домена]

### 9.5. Деплой

Нажмите **"Deploy"** и дождитесь завершения.

**Скриншот:** [Сделайте скриншот успешного деплоя API Gateway]

---

## ✅ Шаг 10: Обновление Frontend (5 минут)

### 10.1. Обновление API URL

1. Откройте файл `frontend/js/api.js`
2. Найдите строку:
   ```javascript
   const API_BASE_URL = window.API_BASE_URL || 'http://localhost:8080';
   ```
3. Замените на ваш домен Railway:
   ```javascript
   const API_BASE_URL = window.API_BASE_URL || 'https://api-gateway-production.up.railway.app';
   ```
   (Замените на ваш реальный домен!)

### 10.2. Коммит изменений

```bash
git add frontend/js/api.js
git commit -m "Update API URL for production"
git push
```

---

## ✅ Шаг 11: Деплой Frontend на Vercel (5 минут)

### 11.1. Регистрация

1. Перейдите на https://vercel.com
2. Войдите через GitHub

### 11.2. Деплой

1. Нажмите **"Add New Project"**
2. Выберите ваш репозиторий
3. Настройки:
   - **Framework Preset:** Other
   - **Root Directory:** `frontend`
   - **Build Command:** (оставьте пустым)
   - **Output Directory:** `frontend`
4. Нажмите **"Deploy"**

### 11.3. Получение домена

Vercel автоматически создаст домен с HTTPS!

**Скриншот:** [Сделайте скриншот деплоя на Vercel]

---

## ✅ Шаг 12: Проверка (2 минуты)

1. Откройте ваш сайт на Vercel
2. Проверьте, что в адресной строке есть 🔒 (HTTPS)
3. Откройте консоль браузера (F12) и проверьте, что нет ошибок
4. Проверьте работу сайта

**Скриншот:** [Сделайте скриншот работающего сайта с HTTPS]

---

## ✅ Шаг 13: Проверка SSL (2 минуты)

1. Откройте https://www.ssllabs.com/ssltest/
2. Введите ваш домен Railway (API Gateway)
3. Дождитесь проверки
4. Должна быть оценка A или выше

**Скриншот:** [Сделайте скриншот проверки SSL]

---

## 🎉 Готово!

Теперь у вас:
- ✅ Все сервисы задеплоены на Railway
- ✅ SSL сертификат установлен автоматически
- ✅ HTTPS работает
- ✅ Frontend задеплоен на Vercel с HTTPS
- ✅ Можно делать скриншоты для отчета

---

## 📸 Скриншоты для отчета

Сделайте скриншоты:
1. Railway Dashboard - все сервисы запущены
2. API Gateway - домен с HTTPS
3. Vercel - Frontend задеплоен
4. Открытый сайт в браузере - с 🔒
5. Проверка SSL на ssllabs.com

---

## 🆘 Если что-то не работает

### Проблема: Сервис не запускается
- Проверьте логи в Railway (вкладка "Deployments" → "View Logs")
- Убедитесь, что все переменные окружения добавлены

### Проблема: Ошибка подключения к БД
- Проверьте, что переменные БД скопированы правильно
- Убедитесь, что БД запущена

### Проблема: Frontend не подключается к API
- Проверьте CORS настройки в API Gateway
- Убедитесь, что URL в `api.js` правильный (с https://)

---

**Время выполнения: ~60-90 минут** ⏱️

