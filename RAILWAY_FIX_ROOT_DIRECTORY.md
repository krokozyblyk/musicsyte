# 🔧 Исправление ошибки: Root Directory для api-gateway

## ❌ Проблема:

Ошибка при сборке:
```
ERROR: "/api-gateway/src": not found
```

Это происходит, потому что **Root Directory** в Railway установлен в `api-gateway`, а Dockerfile пытается найти `api-gateway/src` из корня проекта.

---

## ✅ Решение:

### Вариант 1: Изменить Root Directory (РЕКОМЕНДУЕТСЯ)

1. Откройте Railway Dashboard
2. Перейдите в ваш проект
3. Откройте сервис **`api-gateway`**
4. Перейдите в **Settings**
5. Найдите **Root Directory**
6. Измените с `api-gateway` на `.` (точка = корень проекта)
7. Сохраните изменения
8. Нажмите **"Deploy"** или **"Redeploy"**

**Готово!** Dockerfile из корня проекта теперь будет работать правильно.

---

### Вариант 2: Использовать NIXPACKS (если не хотите менять Root Directory)

Если Root Directory должен остаться `api-gateway`, то нужно использовать NIXPACKS:

1. Откройте `api-gateway/railway.json`
2. Измените на:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/api-gateway-1.0.0.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

**НО:** Это не сработает, потому что нужен родительский `pom.xml` из корня проекта.

---

## 🎯 РЕКОМЕНДАЦИЯ:

**Используйте Вариант 1** - измените Root Directory на `.` (корень проекта) в Railway UI.

Это позволит:
- ✅ Использовать Dockerfile из корня проекта
- ✅ Доступ к родительскому `pom.xml`
- ✅ Правильная сборка multi-module Maven проекта

---

## 📝 После изменения Root Directory:

1. Railway автоматически увидит изменения
2. Запустится новый деплой
3. Сборка должна пройти успешно
4. Сайт обновится через 2-5 минут

---

**Готово! Проблема решена! ✅**

