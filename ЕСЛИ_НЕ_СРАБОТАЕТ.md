# 🔧 ЕСЛИ НЕ СРАБОТАЛО - АЛЬТЕРНАТИВНЫЕ РЕШЕНИЯ

## ❌ Если Root Directory = `.` не помогло:

---

## ✅ РЕШЕНИЕ 1: Использовать Dockerfile из api-gateway

### Настройки в Railway:

```
Root Directory: api-gateway
Builder: Dockerfile
Dockerfile Path: Dockerfile (БЕЗ api-gateway/)
Start Command: (ПУСТО)
```

**Почему:** Dockerfile в `api-gateway/Dockerfile` уже правильно настроен для Root Directory = `api-gateway`.

---

## ✅ РЕШЕНИЕ 2: Изменить Dockerfile в корне

Если Root Directory = `.`, но все еще не работает, обновите Dockerfile в корне:

**Измените:**
```dockerfile
COPY api-gateway/pom-standalone.xml /app/pom.xml
COPY api-gateway/src /app/src
```

**На:**
```dockerfile
COPY pom-standalone.xml /app/pom.xml
COPY src /app/src
```

Но это работает только если Root Directory = `api-gateway`.

---

## ✅ РЕШЕНИЕ 3: Использовать NIXPACKS (проще всего!)

### Настройки в Railway:

```
Root Directory: api-gateway
Builder: NIXPACKS (или Railpack)
Build Command: mvn clean package -DskipTests
Start Command: java -jar target/api-gateway-1.0.0.jar
```

**Проблема:** Нужен родительский pom.xml, но можно использовать pom-standalone.xml.

---

## ✅ РЕШЕНИЕ 4: Создать универсальный Dockerfile

Создать Dockerfile, который работает в обоих случаях (Root Directory = `.` или `api-gateway`).

---

## 🎯 РЕКОМЕНДАЦИЯ:

**Попробуйте РЕШЕНИЕ 1** - это самое простое и надежное:

1. Root Directory: `api-gateway`
2. Dockerfile Path: `Dockerfile` (без `api-gateway/`)
3. Builder: `Dockerfile`

Это должно работать, потому что `api-gateway/Dockerfile` уже правильно настроен!

---

## 📞 Если ничего не помогло:

1. Проверьте логи в Railway (вкладка "Logs")
2. Убедитесь, что файлы `api-gateway/pom-standalone.xml` и `api-gateway/src` существуют в репозитории
3. Попробуйте пересоздать сервис в Railway с нуля

---

**Попробуйте РЕШЕНИЕ 1 - оно должно сработать! ✅**

