# 🔧 Исправление ошибки: "The executable `bash` could not be found"

## ❌ Проблема:

Ошибка при деплое:
```
The executable `bash` could not be found.
```

Это происходит, потому что в настройках Railway указан **Start Command** с `bash` или `start.sh`, а в Alpine Linux (который используется в Dockerfile) нет `bash`.

---

## ✅ Решение: Изменить настройки в Railway UI

### Шаг 1: Откройте настройки сервиса

1. Откройте Railway Dashboard: https://railway.app
2. Перейдите в ваш проект
3. Откройте сервис **`api-gateway`**
4. Перейдите в **Settings**

### Шаг 2: Проверьте и исправьте настройки

#### 2.1. Root Directory
- **Должно быть:** `.` (точка = корень проекта)
- **ИЛИ:** `api-gateway` (если используете Dockerfile из api-gateway)

#### 2.2. Builder
- **Должно быть:** `DOCKERFILE`
- **НЕ должно быть:** `NIXPACKS`

#### 2.3. Build Command
- **Оставьте ПУСТЫМ** (используется из Dockerfile)
- **ИЛИ удалите**, если там что-то указано

#### 2.4. Start Command ⚠️ ВАЖНО!
- **УДАЛИТЕ** команду, если там указано:
  - `bash api-gateway/start.sh`
  - `bash start.sh`
  - Любая команда с `bash`
- **Оставьте ПУСТЫМ** (используется из Dockerfile: `ENTRYPOINT ["java", "-jar", "app.jar"]`)
- **ИЛИ** укажите напрямую: `java -jar app.jar`

#### 2.5. Dockerfile Path
- **Должно быть:** `api-gateway/Dockerfile`
- **ИЛИ:** `Dockerfile` (если Root Directory = `.`)

### Шаг 3: Сохраните и передеплойте

1. Нажмите **"Save"** или **"Update"**
2. Нажмите **"Deploy"** или **"Redeploy"**

---

## 📋 Правильные настройки для api-gateway:

```
Service Name: api-gateway
Root Directory: api-gateway (или .)
Builder: DOCKERFILE
Dockerfile Path: api-gateway/Dockerfile
Build Command: (пусто)
Start Command: (пусто)
```

---

## 🔍 Как проверить текущие настройки:

1. В Railway откройте сервис `api-gateway`
2. Перейдите в **Settings**
3. Проверьте каждую настройку из списка выше
4. Если что-то не совпадает - исправьте

---

## ⚠️ ВАЖНО:

- **НЕ используйте** `bash` в командах
- **НЕ используйте** `start.sh` (он использует bash)
- **Используйте** прямую команду `java -jar app.jar` или оставьте пустым (используется из Dockerfile)

---

## ✅ После исправления:

Railway автоматически:
1. Использует команду из Dockerfile (`ENTRYPOINT ["java", "-jar", "app.jar"]`)
2. Запустит приложение без ошибок
3. Сайт будет работать

---

**Готово! Ошибка должна исчезнуть! ✅**

