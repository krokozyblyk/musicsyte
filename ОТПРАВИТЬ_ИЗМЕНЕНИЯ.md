# 📤 Как отправить изменения в GitHub

## 🚀 Быстрая команда (все изменения):

```bash
git add .
git commit -m "Исправил Dockerfile для api-gateway"
git push
```

---

## 📝 Пошагово:

### 1. Добавить все изменения:
```bash
git add .
```

### 2. Создать коммит:
```bash
git commit -m "Исправил Dockerfile для api-gateway"
```

### 3. Отправить в GitHub:
```bash
git push
```

---

## ⚠️ ВАЖНО:

После `git push` Railway автоматически:
- Увидит изменения
- Запустит сборку
- Обновит сайт через 2-5 минут

---

## 🔍 Проверка:

После `git push` проверьте в Railway:
1. Откройте ваш проект
2. Перейдите в сервис `api-gateway`
3. Во вкладке **"Deployments"** увидите новый деплой
4. Статус: **"Building"** → **"Deploying"** → **"Active"**

---

**Готово! Сайт обновится автоматически! ✅**

